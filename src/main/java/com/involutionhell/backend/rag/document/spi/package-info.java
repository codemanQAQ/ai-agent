/**
 * Internal collaboration contract exported by the document module.
 *
 * <p>This named interface exists specifically for the indexing module. It allows indexing to
 * read and update document indexing state without depending on document persistence types or
 * repository implementations directly.
 */
@org.springframework.modulith.NamedInterface("spi")
package com.involutionhell.backend.rag.document.spi;
