-- psql bootstrap script.
-- Run this while connected to a maintenance database such as postgres:
-- psql "postgresql://USER:PASSWORD@HOST:5432/postgres" -f create-database.psql

SELECT 'CREATE DATABASE ecommerce_offline'
WHERE NOT EXISTS (
    SELECT 1
    FROM pg_database
    WHERE datname = 'ecommerce_offline'
)\gexec
